import { env } from "cloudflare:workers";
import { and, eq } from "drizzle-orm";
import { requireUserOrResponse } from "../../../../../../lib/auth/current-user";
import { getDb } from "../../../../../../db";
import { deals, documentAuditEvents, documentFiles } from "../../../../../../db/schema";
export async function GET(request:Request,{params}:{params:Promise<{id:string;fileId:string}>}){const auth=await requireUserOrResponse(request);if(auth instanceof Response)return auth;const user=auth;const p=await params,dealId=Number(p.id),fileId=Number(p.fileId),db=getDb();const [[deal],[file]]=await Promise.all([db.select().from(deals).where(and(eq(deals.id,dealId),eq(deals.ownerEmail,user.email))).limit(1),db.select().from(documentFiles).where(and(eq(documentFiles.id,fileId),eq(documentFiles.dealId,dealId),eq(documentFiles.fileStatus,"active"))).limit(1)]);if(!deal||!file)return Response.json({error:"File not found."},{status:404});const object=await env.BUCKET.get(file.storageKey);if(!object)return Response.json({error:"Stored file unavailable."},{status:404});await db.insert(documentAuditEvents).values({documentFileId:file.id,dealId,actorEmail:user.email,action:"downloaded",details:"Owner download"});return new Response(object.body,{headers:{"content-type":file.contentType,"content-disposition":`attachment; filename="${file.originalName.replace(/["\\]/g,"_")}"`,"cache-control":"private, no-store","x-content-type-options":"nosniff"}})}
