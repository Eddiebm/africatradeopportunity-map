// Ambient types for the virtual `cloudflare:test` module (env, SELF,
// applyD1Migrations, D1Migration, ...) used across tests/unit/*.test.ts.
/// <reference types="@cloudflare/vitest-pool-workers/types" />
